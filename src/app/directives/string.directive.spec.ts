import { StringDirective } from './string.directive';

describe('StringDirective', () => {
  it('should create an instance', () => {
    const directive = new StringDirective();
    expect(directive).toBeTruthy();
  });
});
