import { ExistDirective } from './exist.directive';

describe('ExistDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistDirective();
    expect(directive).toBeTruthy();
  });
});
