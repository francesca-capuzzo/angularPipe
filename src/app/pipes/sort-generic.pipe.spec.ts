import { SortGenericPipe } from './sort-generic.pipe';

describe('SortGenericPipe', () => {
  it('create an instance', () => {
    const pipe = new SortGenericPipe();
    expect(pipe).toBeTruthy();
  });
});
